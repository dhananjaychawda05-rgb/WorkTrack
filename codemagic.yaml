workflows:
  android-workflow:
    name: Android Workflow
    max_build_duration: 60
    environment:
      groups:
        - android_signing
      flutter: stable
      java: 17

    triggering:
      events:
        - push
        - pull_request
      branch_patterns:
        - pattern: main
          include: true
        - pattern: develop
          include: true

    scripts:
      - name: Set up keystore
        script: |
          # Decode without line breaks to prevent corruption
          echo $CM_KEYSTORE | base64 --decode -w 0 > $CM_BUILD_DIR/my-release-key.jks || \
          echo $CM_KEYSTORE | base64 -d > $CM_BUILD_DIR/my-release-key.jks

          # Verify keystore is valid
          keytool -list \
            -keystore $CM_BUILD_DIR/my-release-key.jks \
            -storepass $CM_KEYSTORE_PASSWORD \
            -v | head -5

          # Write key.properties for Gradle
          cat > "$CM_BUILD_DIR/android/key.properties" <<EOF
          storePassword=$CM_KEYSTORE_PASSWORD
          keyPassword=$CM_KEY_PASSWORD
          keyAlias=$CM_KEY_ALIAS
          storeFile=$CM_BUILD_DIR/my-release-key.jks
          EOF

      - name: Get Flutter packages
        script: flutter pub get

      - name: Run tests
        script: flutter test

      - name: Build release APK
        script: |
          flutter build apk --release \
            --build-name=1.0.0 \
            --build-number=$BUILD_NUMBER

    artifacts:
      - build/**/outputs/**/*.apk
      - flutter_drive.log

    publishing:
      email:
        recipients:
          - your@email.com
        notify:
          success: true
          failure: true